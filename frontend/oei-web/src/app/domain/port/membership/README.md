# Bounded context: `membership` (ports)

Ports pour la lecture du niveau/statut d'adhésion et la demande de rattachement
employeur.

Endpoints OpenAPI correspondants : `GET /api/member/v1/membership`,
`GET|POST /api/member/v1/employment-affiliations`.
