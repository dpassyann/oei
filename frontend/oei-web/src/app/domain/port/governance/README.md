# Bounded context: `governance` (ports)

Ports pour les contributions membres et la synchronisation Git en lecture.

Endpoints OpenAPI correspondants : `GET|POST /api/member/v1/contributions`,
`GET /api/admin/v1/content/{id}/contributions`,
`POST /api/admin/v1/git/synchronize`, `GET /api/admin/v1/git/synchronizations`.
