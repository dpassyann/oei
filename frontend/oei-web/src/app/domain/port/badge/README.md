# Bounded context: `badge` (ports)

Ports pour lire le catalogue public de badges et les badges obtenus par le membre
connecté.

Endpoints OpenAPI correspondants : `GET /api/public/v1/badges/catalog`,
`GET /api/member/v1/badges`.
