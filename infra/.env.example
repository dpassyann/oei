# Copier ce fichier en infra/.env et remplacer les valeurs avant de lancer docker-compose.
# infra/.env est gitignoré — ne jamais committer de vrais secrets.
#
# Un seul mot de passe (OEI_PASSWORD) est réutilisé par tous les conteneurs locaux
# (Postgres, Keycloak, MinIO) pour simplifier le développement local — convention
# reprise du projet aegis (AEGIS_USER/AEGIS_PASSWORD partagés entre tous ses services).

OEI_USER=oei
OEI_PASSWORD=changeme

POSTGRES_DB=keycloak

MINIO_ROOT_USER=oei-minio-admin
