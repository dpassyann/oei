[(#{email.renewal-reminder.greeting(${member.displayName()})})]

[(#{email.renewal-reminder.body(${cycleYear})})]

[(#{email.renewal-reminder.cta})] [(${publicBaseUrl})]/espace-membre/cotisation

[(#{email.renewal-reminder.signature-text})]
