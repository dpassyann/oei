[(#{email.order-confirmation.greeting(${member.displayName()})})]

[(#{email.order-confirmation.body(${order.id()}, ${order.totalAmount()}, ${order.totalCurrency()})})]

[(#{email.order-confirmation.fulfillment-note})]

[(#{email.order-confirmation.signature})]
