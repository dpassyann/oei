spring:
  config:
    activate:
      on-profile: stripe

oei:
  payment:
    stripe:
      api-base-url: ${OEI_STRIPE_API_BASE_URL:https://api.stripe.com/v1}
      api-key: ${OEI_STRIPE_API_KEY:sk_test_placeholder}

